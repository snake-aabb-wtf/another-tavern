{
  "name": "@another-tavern/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "dev": "pnpm build && node dist/main.js",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@another-tavern/engine": "workspace:*",
    "@another-tavern/sse": "workspace:*",
    "@hono/node-server": "^1.19.17",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "png-chunk-text": "^1.0.0",
    "png-chunks-encode": "^1.0.0"
  },
  "license": "MIT",
  "files": [
    "dist",
    "drizzle"
  ]
}
